{
  "name": "la-council",
  "private": false,
  "version": "0.0.0",
  "description": "Signal-first LA City Council legibility assistant (MVP: web research + citations)",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(stub) lint not configured yet\"",
    "dev": "vitest",
    "dev:api": "HTTP_HOST=127.0.0.1 HTTP_PORT=8787 tsx src/index.ts"
  },
  "dependencies": {
    "dotenv": "^17.2.2",
    "fastify": "^5.6.0",
    "openai": "^5.23.0",
    "zod": "^4.1.9"
  },
  "devDependencies": {
    "@types/node": "^24.4.0",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}


